import assert from 'assert';
import solution from './solution';

assert.equal(0, solution(0));
assert.equal(1, solution(1));
assert.equal(1, solution(2));
assert.equal(5, solution(5));
assert.equal(55, solution(10));
